"""
This package handles the reactivity features for the application.

Modules:
    buttons_controller: Manages the button operations including recording, saving, switching modes, converting datasets, and deleting sessions.
    metadata_controller: Handles the metadata editing and storing functionality for the application.
    dataset_controller: Manages the dataset operations including editing metadata, deleting sessions, and converting datasets.

Classes:
    Buttons: Manages the button operations including recording, saving, switching modes, converting datasets, and deleting sessions.
    MetadataController: Sets up the reactive effects for editing, storing, and resetting metadata.
    StorageController: Manages the dataset operations including editing metadata, deleting sessions, and converting datasets.
"""