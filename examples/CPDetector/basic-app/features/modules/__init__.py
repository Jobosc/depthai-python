"""
This module provides a summary of all classes and their purposes in the modules folder.

Classes:
    LightBarrier: Manages the light barrier sensor operations including initialization and state checking.
    Participant: Manages participant metadata including ID, comments, and timestamps.
    TimeWindow: Represents a time interval with a start and end datetime.
    Timestamps: Represents the timestamps for recording sessions, including activation time, camera start time, and time windows.
    UIState: Manages the UI state including users, sessions, days recorded, and unsaved days.
    CameraLed: Manages the LED status updates for the camera.
"""
